# Custom changes vs the official build

- **On-demand card image downloading**: Missing card images are automatically downloaded from Scryfall in the background when first displayed, instead of requiring a bulk download upfront. Enable via "Download missing images automatically" in Preferences > Images. Card panels auto-refresh once the image arrives.
